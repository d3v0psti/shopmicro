# Copie para .env (mesma pasta do docker-compose-managed-db.yml) e preencha.
# Nunca commite o .env de verdade — só este .example.
DB_CONNECTION_STRING=Host=SEU-HOST-AQUI;Port=5432;Database=shopdb;Username=postgres;Password=SUA_SENHA;SSL Mode=VerifyFull;Root Certificate=/app/certs/SEU_CERTIFICADO.pem
