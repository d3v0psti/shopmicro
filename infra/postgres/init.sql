-- Este script é opcional: as migrations do EF Core (backend) já criam
-- e mantêm o schema automaticamente ao subir a aplicação.
-- Use este arquivo apenas se quiser popular dados de exemplo em dev.

-- INSERT INTO tasks (id, title, description, is_completed, created_at)
-- VALUES (gen_random_uuid(), 'Configurar ambiente', 'Rodar docker-compose up', false, now());
